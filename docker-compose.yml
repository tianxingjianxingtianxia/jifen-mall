version: '3.8'

services:
  jifen-backend:
    build:
      context: ./jifen-backend
      dockerfile: Dockerfile
    container_name: jifen-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://host.docker.internal:3306/wj_jifen?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&zeroDateTimeBehavior=convertToNull
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=root123
      - SPRING_DATA_REDIS_HOST=host.docker.internal
      - SPRING_DATA_REDIS_PORT=6379
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: jifen-frontend
    ports:
      - "80:80"
    volumes:
      - ./jifen-frontend/dist:/usr/share/nginx/html:ro
    depends_on:
      - jifen-backend
    restart: unless-stopped
