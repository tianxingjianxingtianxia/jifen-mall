server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: jifen-backend
  datasource:
    url: jdbc:mysql://192.168.1.49:3306/wj_jifen?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&zeroDateTimeBehavior=convertToNull
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-init-sql: SET NAMES utf8mb4
  data:
    redis:
      host: 192.168.1.49
      port: 6379
      password:
      timeout: 3000

mybatis-plus:
  global-config:
    db-config:
      id-type: AUTO
      logic-delete-field: isDeleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: jifen-mall-secret-key-2026-this-is-for-dev-only-do-not-use-in-prod
  expiration: 86400000  # 24 hours
